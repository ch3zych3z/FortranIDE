package view.main

import javafx.scene.control.MenuBar
import javafx.scene.control.TabPane

class ProjectMenu {
    fun setupProjectMenu(): TabPane{
        val tabPane = TabPane()
        return tabPane
    }
}